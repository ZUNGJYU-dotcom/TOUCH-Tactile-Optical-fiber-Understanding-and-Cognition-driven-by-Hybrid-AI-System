# Portable Release

The Windows build for this edition must be named `TOUCH System - FBG
Wavelength Shift` and use local port 8630.

Keep the executable beside its `_internal` directory. Live acquisition also
requires the matching BaySpec driver and vendor SDK/runtime on the target
computer. The portable package must not contain or replace the PD-voltage or
optical-intensity editions.
